{
  "name": "chinese-independent-blogs-rss",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Aggregate posts from timqian/chinese-independent-blogs into a single RSS feed.",
  "scripts": {
    "update": "node scripts/update.js",
    "lint": "node --check scripts/update.js && node --check src/*.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "p-limit": "^6.2.0",
    "rss-parser": "^3.13.0",
    "sanitize-html": "^2.17.4"
  },
  "engines": {
    "node": ">=20"
  }
}
